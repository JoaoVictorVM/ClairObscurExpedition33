export interface City {
  id: string;
  name: string;
  backgroundImage: string;
}

export interface Testimonial {
  id: string;
  image: string;
  imageAlt: string;
  text: string;
}

export interface SocialLink {
  icon: string;
  alt: string;
}
